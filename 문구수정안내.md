# TestWell 문구 수정 안내

간단한 수정 절차와 워드 수정표 이용 방법은 [두 가지 수정 방법 안내](교수님께_전달드리는_안내.md)에 정리해 두었습니다. 아래 내용은 직접 편집하거나 미리보기를 실행하실 때 참고하실 상세 안내입니다. 문구 파일만 수정하여 보내주시는 경우에는 미리보기를 설치하거나 실행하지 않으셔도 됩니다.

교수님께서 랜딩페이지의 문구를 직접 수정하실 수 있도록, 본문에 표시되는 문구를 **[src/constants/content.ts](src/constants/content.ts)** 파일에 모아두었습니다. 아래 안내에 따라 문구를 수정하시면 디자인과 버튼 연결 주소는 그대로 유지됩니다.

## 1. 수정하실 항목 확인

수정하시려는 화면 위치에 따라 아래 항목을 찾아주시면 됩니다.

| 화면 위치 | 파일 안에서 찾을 항목 |
|---|---|
| 맨 위 메뉴 | `header` → `label`, `login` |
| 첫 화면 제목·설명 | `hero` → `title`, `description` |
| 첫 화면 버튼·아래 안내 | `hero` → `primary`, `note`, `ticket`, `ticketNote`, `browse`, `browseNote`, `result`, `resultNote` |
| 응시권 입력란·버튼·안내 | `ticketForm` |
| 주요 기능 제목·설명·원 안의 이름 | `capabilities` → `title`, `description`, `displayItems` |
| 문항 준비 설명 | `measurement` |
| 하단 검사 탐색·문의 | `finalCta` |
| 맨 아래 설명·저작권 문구 | `footer` |

`label`은 작은 제목, `title`은 큰 제목, `description`은 설명에 해당합니다. `menuOpen`, `menuClose`, `navLabel`, `listLabel`, `homeLabel`, `skip`은 화면 읽기 프로그램 등에서 사용하는 접근성 안내이므로, 별도로 변경하실 필요가 없다면 그대로 두셔도 됩니다.

## 2. 문구 수정 방법

예를 들어, 첫 화면의 버튼 문구는 다음과 같이 입력되어 있습니다.

```ts
primary: '검사 만들기',
```

버튼 이름을 변경하시려면 따옴표 안의 문구를 다음과 같이 수정하시면 됩니다.

```ts
primary: '검사 제작하기',
```

- 따옴표 **안의 글자만** 수정해 주시면 됩니다. 왼쪽 영문 항목명과 따옴표·쉼표·괄호는 그대로 유지해 주시기 바랍니다.
- `link: 'about'`과 같은 **link 값은 문구가 아니라 버튼 연결 설정**이므로, 변경하지 않고 그대로 두시면 됩니다.
- 첫 화면의 `title`은 두 문장을 각각 표시하도록 구성되어 있습니다. 기존 두 항목은 유지하시고, 각 항목의 문구를 수정해 주시면 됩니다.
- 기능 제목의 `\n`은 줄바꿈 표시입니다. 다른 일반 설명은 화면 폭에 따라 자동 줄바꿈됩니다.
- 문장 안에 작은따옴표가 필요하시면 `\'` 또는 ‘ ’를 사용하실 수 있습니다. 예: `'‘검사 보기’를 선택하세요.'`
- 원형 기능 이름은 아이콘에 맞춰 배치되어 있으므로 **6개 항목과 순서는 유지**해 주시기 바랍니다. 현재와 비슷한 길이로 작성하시면 보기 좋게 표시됩니다. 문구가 길어지는 경우에는 작은 화면에서도 확인해 주시면 좋겠습니다.
- 수정하신 내용은 `Ctrl+S`로 저장하실 수 있습니다. 실수로 내용을 변경하셨다면 `Ctrl+Z`로 직전 수정부터 되돌리실 수 있습니다.

수정하실 항목을 찾기 쉽도록 현재 화면에서 사용하지 않는 예전 문구는 편집 파일에서 제외했습니다. 로고 뒤의 점과 저작권 기호·연도는 자동으로 표시되므로 별도로 입력하지 않으셔도 됩니다. 빈 입력란에 표시되는 브라우저 기본 오류 메시지는 브라우저의 언어 설정을 따릅니다.

## 3. 컴퓨터에서 미리보기 실행

미리보기를 실행하시려면 Node.js와 코드 편집기(예: VS Code)가 필요합니다. 전달받으신 프로젝트 폴더 전체를 편집기에서 열어주시면 됩니다. 처음 실행하시며 `node_modules` 폴더가 없는 경우에는 프로젝트 터미널에 아래 명령을 입력해 주시기 바랍니다.

```sh
npm ci
```

설치가 완료되면 아래 명령으로 미리보기를 실행하실 수 있습니다.

```sh
npm run dev -- --host 127.0.0.1 --port 4173 --strictPort
```

브라우저에서 **http://127.0.0.1:4173/** 에 접속하시면 미리보기를 확인하실 수 있습니다. 터미널을 켜둔 상태에서 문구를 저장하시면 수정 내용이 반영됩니다. 이전 문구가 보이는 경우에는 새로고침해 주시기 바랍니다. 포트가 이미 사용 중이라는 오류가 표시되면 기존 미리보기를 이용하시거나, 해당 터미널에서 `Ctrl+C`로 종료하신 뒤 다시 실행하시면 됩니다.

수정을 마치신 뒤에는 아래 명령으로 파일의 문법 오류 여부를 확인하실 수 있습니다.

```sh
npm run build
```

오류가 표시되면 안내된 파일명과 줄 번호를 참고하여 따옴표·쉼표·괄호가 유지되어 있는지 확인해 주시기 바랍니다. 문법 검사를 통과하더라도 문구가 길면 배치가 달라질 수 있으므로, 브라우저 폭을 넓히거나 좁혀 버튼과 원 안의 글자가 잘 보이는지도 확인해 주시면 좋겠습니다.

## 4. 수정본 전달과 실제 반영

문구만 수정하신 경우에는 `src/constants/content.ts` 파일을 보내주시면 됩니다. 작업하신 버전의 커밋 번호 또는 프로젝트를 전달받으신 날짜도 함께 알려주시면, 서로 다른 버전을 덮어쓰지 않도록 변경 내용을 비교하여 반영하는 데 도움이 됩니다.

GitHub에서 직접 수정하시는 경우에는 편집 권한이 필요합니다. `codex/landing-v3-reference-led` 브랜치를 선택하신 뒤 해당 파일을 열어 수정하실 수 있습니다. 공동 작업 중에는 새 브랜치와 Pull Request를 통해 수정본을 전달해 주시기 바랍니다. GitHub에 저장하는 것만으로 로컬 미리보기 검사가 실행되지는 않습니다.

**파일 저장, GitHub 반영, 실제 testwell.kr 배포는 서로 다른 단계입니다.** 수정하신 내용을 실제 서비스에 반영하려면 운영 담당자의 빌드·배포 작업이 필요합니다. 공개 검사 목록의 배포 후 경로와 문의 연결도 운영 담당자가 확인할 사항으로 남아 있습니다.

이 안내는 랜딩페이지 본문의 문구 수정을 기준으로 작성했습니다. 브라우저 탭 제목·검색 및 공유 설명은 `index.html`, 버튼 주소는 `src/constants/links.ts`에서 별도로 관리하고 있습니다. 실제 검사 화면과 결과지는 이번 편집 파일의 수정 범위에 포함되지 않습니다.
